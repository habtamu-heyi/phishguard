# PhishGuard Detection Validation Study — Deep Mode (Structural + Domain Age)

**Conducted:** 2026-09-09T22:46:19.461Z
**Author:** Habtamu Heyi
**Mode:** Deep — structural engine + domain age enrichment
**Engine tested:** PhishGuard URL Engine v1.0 + Domain Age Enrichment v1.0 (src/engines/url-engine.js + src/domain-age.js)

## Methodology

A sample of 100 URLs was drawn at random, without replacement, from PhishTank's live public feed of independently verified phishing URLs (https://data.phishtank.com/data/online-valid.csv). PhishTank is a community-driven, independently operated phishing verification database; every URL in its feed has already been confirmed as phishing by reviewers unaffiliated with this project prior to being included in this study.

The raw sample was saved to disk (`phishtank-raw-sample-deep-2026-09-09.json`) immediately after being drawn and before any scoring occurred.

## Domain Age Enrichment

This run adds a domain-age signal (src/domain-age.js) on top of the base structural engine, using free RDAP registration lookups. This is a deliberately separate, additive layer — the base 6-dimension engine and its weights are unchanged from the original study; domain age is blended in afterward at a fixed weight of 0.25 (see src/domain-age.js for rationale).

| Metric | Value |
|---|---|
| RDAP lookup success rate | 0.0% (0/100) |
| Detection rate — structural signal only | 2.0% |
| Detection rate — structural + domain age (blended) | 2.0% |
| Improvement from domain age signal | 0.0 percentage points |
| Average registered age of sampled domains | N/A days |

## Results

| Metric | Value |
|---|---|
| Total URLs sampled | 100 |
| Successfully parsed and scored | 100 |
| Parse errors | 0 |
| **Detection rate (score ≥ 40, SUSPICIOUS or higher)** | **2.0%** |
| High-confidence detection rate (score ≥ 65, PHISHING) | 0.0% |
| Average PhishScore across sample | 11.9/100 |
| False negatives | 98 |

## Detection Dimension Averages

- **lexical analysis**: 1.2/100 average
- **domain entropy**: 8.9/100 average
- **brand impersonation**: 16.1/100 average
- **tld risk**: 25.6/100 average
- **structural anomalies**: 2.4/100 average
- **redirect risk**: 7.5/100 average

## Limitations

Domain age data is not available for all domains — some registries do not support RDAP, or return incomplete records, and these lookups fail gracefully without penalizing the affected URL. The domain age signal, while a strong general predictor, does not by itself resolve every case of a phishing site hosted on a long-registered, compromised legitimate domain, since in that scenario the domain's registration date reflects its original legitimate owner, not the attacker.

## Reproducibility

This study can be reproduced by running `npm run validate:deep` from the PhishGuard repository root.

---
*Generated by `scripts/phishtank-validation.js --deep`. Raw data: `validation-study-deep-2026-09-09.json`, `phishtank-raw-sample-deep-2026-09-09.json`.*
